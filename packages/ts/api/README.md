# @contextcompany/api

This package contains shared functionality and core API utilities used by all @contextcompany TypeScript packages.

For setup instructions, see our [documentation](https://docs.thecontext.company/).

Note: This package is typically installed as a dependency of other @contextcompany packages and doesn't need to be installed directly.