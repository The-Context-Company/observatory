---
phase: 07.1-move-cli-routes-from-dashboard-to-public-api
plan: 02
subsystem: liftoff-cli
tags: [cli, url-migration, api-routing]
dependency_graph:
  requires: []
  provides: [liftoff-cli-points-to-public-api]
  affects: [auth-step, provision-keys-step, instrument-step, setup-slack-step]
tech_stack:
  added: []
  patterns: [API_BASE constant pattern for public-api URLs]
key_files:
  created: []
  modified:
    - packages/ts/liftoff/src/steps/auth.ts
    - packages/ts/liftoff/src/steps/provision-keys.ts
    - packages/ts/liftoff/src/steps/instrument.ts
    - packages/ts/liftoff/src/steps/setup-slack.ts
decisions:
  - User-facing dashboard URLs (settings page, org page) remain as www.thecontext.company since those are browser destinations, not API endpoints
  - Updated JSDoc comment in instrument.ts tryAIInstrumentation to reflect new /cli/instrument path
metrics:
  duration: 55s
  completed: 2026-03-31T08:58:28Z
---

# Phase 07.1 Plan 02: Update Liftoff CLI Base URLs Summary

Replaced DASHBOARD_BASE (www.thecontext.company) with API_BASE (api.thecontext.company) in all 4 liftoff step files and updated fetch paths from /api/cli/* to /cli/*

## Changes Made

### Task 1: Update all liftoff step base URLs from DASHBOARD_BASE to API_BASE

**Commit:** 77966aa

In all 4 step files, replaced the constant declaration and updated every fetch URL:

- **auth.ts**: `API_BASE` with `/cli/auth/start` and `/cli/auth` paths (2 fetch calls)
- **provision-keys.ts**: `API_BASE` with `/cli/keys` path (1 fetch call)
- **instrument.ts**: `API_BASE` + `DEV_API_BASE` with `/cli/instrument` path (1 fetch call, dev/prod branching preserved)
- **setup-slack.ts**: `API_BASE` with `/cli/slack-client-id` and `/cli/slack-callback` paths (2 fetch calls)

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Fixed stale JSDoc comment in instrument.ts**
- **Found during:** Task 1 verification
- **Issue:** The `tryAIInstrumentation` JSDoc still referenced `/api/cli/instrument` after the URL was updated
- **Fix:** Updated comment to `/cli/instrument`
- **Files modified:** packages/ts/liftoff/src/steps/instrument.ts
- **Commit:** 77966aa (included in same commit)

## Known Stubs

None -- all changes are complete URL replacements with no placeholder values.

## Verification

- Zero occurrences of `DASHBOARD_BASE` in the 4 target files
- Zero occurrences of `/api/cli/` in fetch URLs across all 4 files
- All 4 files contain `api.thecontext.company` in their base URL constant
- instrument.ts contains both `API_BASE` and `DEV_API_BASE`

## Self-Check: PASSED
